# Nuxt dev/build outputs
.output
.data
.nuxt
# Only when a build is pointed here with NUXT_BUILD_DIR; otherwise everything writes `.nuxt`.
.nuxt-build
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Demo Factory runs published by `yarn demo:publish:web` in src/app-server-ts:
# rendered clips and narration, reproducible from the demo.yaml.
public/demo-factory
